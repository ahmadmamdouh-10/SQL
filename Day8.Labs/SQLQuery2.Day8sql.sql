-- 1. Create an index on column (Hiredate) that allows you to cluster
--    the data in the table Department. What will happen?
-- 2. Create an index that allows you to enter unique ages in the
--    student table. What will happen?
-- 3. Create a non-clustered index on column(Manager_hiredate)
--     that allows you to enter a unique instructor id in the table
--   Department.

-- 4.Create a cursor for Employee table that increases Employee salary by 10% if Salary <3000 and increases it by 20% if Salary >=3000. Use company DB 

-- 5.Display Department name with its manager name using       cursor. Use ITI DB 

-- 6.Create full, differential Backup for  Company_SD DB. 

-- 7.Use import export wizard to display students data (ITI DB) in excel sheet (self study)

-- 8.Try to generate script from DB ITI that describes all tables and views in this DB 

-- 9.Create job for backup ITI DB every day at 12:00PM 






-- 10.What is the difference between the following objects in SQL Server 
-- 1.	batch, script and transaction 

-- 2.	trigger and stored procedure 

-- 3.	stored procedure and functions 

-- 4.	drop, truncate and delete statement 

-- 5.	select and select into statement 

-- 6.	local and global variables 

-- 7.	convert and cast statements 

--8.	DDL,DML,DCL,DQL and TCL 

--9.	For xml raw and for xml auto 

--10.	Table valued and multi statemcent function

--11.	Varchar(50) and varchar(max) 

--12.	Datetime(3), datetime2(7) and datetimeoffset(7) 

--13.	Default instance and named instance 

--14.	SQL and windows Authentication 

--15.	Clustered and non-clustered index 

--16.	Group by rollup and group by cube 

--17.	Sequence object and identity  

--18.	Inline table function and view 

--19.	Table variable and temporary table 

--20.	Row_number() and dense_Rank() function 


